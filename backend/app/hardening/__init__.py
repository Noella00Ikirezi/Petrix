"""Package HCO — Hardening Configuration Operator.

Exporte le moteur d'audit (engine), le connecteur SSH et la table de
correspondance CVE. Les modules d'audit par OS sont dans le sous-package
``modules/``.
"""
